import React from "react";

const AttributeManagement: React.FC = () => {
  return (
    <div>
      <h2 className="text-lg font-medium">Attribute Management (stub)</h2>
      <p className="text-sm text-muted-foreground">Componente temporal para completar el build.</p>
    </div>
  );
};

export default AttributeManagement;
