// Iconos centralizados para optimizar tree shaking de lucide-react
// Solo importa los iconos que realmente se usan

// Navegación y UI básica
export {
  Search,
  User,
  HelpCircle,
  Menu,
  X,
  LogIn,
  LogOut,
  Settings,
  Home,
  ArrowLeft,
  ArrowRight,
  ChevronLeft,
  ChevronRight,
  PanelLeft,
} from "lucide-react";

// Carrito y comercio
export { ShoppingCart, ShoppingBag, Package, Plus, Minus, Trash2, Heart, Gift } from "lucide-react";

// Citas y calendario
export { Calendar, Clock, MapPin, Phone, Mail } from "lucide-react";

// Estados y feedback
export { CheckCircle, CheckCircle2, XCircle, AlertCircle, AlertTriangle, Loader2, Check } from "lucide-react";

// Productos y características
export { Star, Trophy, TrendingUp, Sparkles, Eye } from "lucide-react";

// Transporte y entrega
export { Truck, Receipt } from "lucide-react";

// Seguridad y usuario
export { Shield, UserPlus } from "lucide-react";
